<% if article.favorited_by?(current_user) %>
    <%= link_to article_favorites_path(article), method: :delete, remote: true do %>
      <i class="fab fa-pinterest mark-button"></i><%= article.favorites.count %>mark
    <% end %>
<% else %>
    <%= link_to article_favorites_path(article), method: :post, remote: true do %>
      <i class="fab fa-pinterest-p mark-button"></i><%= article.favorites.count %>mark
    <% end %>
<% end %>
